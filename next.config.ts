import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['photojewels.in','example.com','encrypted-tbn0.gstatic.com'],
  },
};


export default nextConfig;
